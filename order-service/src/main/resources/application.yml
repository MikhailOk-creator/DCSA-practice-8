server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/order_database
    username: postgres
    password: su
  jpa:
    hibernate:
      ddl-auto: update