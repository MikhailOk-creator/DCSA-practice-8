server:
  port: 8082

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/inventory_database
    username: postgres
    password: su
  jpa:
    hibernate:
      ddl-auto: update
